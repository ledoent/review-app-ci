# Committed DEFAULTS so `kustomize build` works at rest; CI overwrites this
# file per deploy (review-app-ci actions/write-params).
REVIEW_HOST={{ app_name }}-review1.{{ review_domain }}
REVIEW_URL=https://{{ app_name }}-review1.{{ review_domain }}
PR_NUMBER=0
REVIEW_SLOT=1
